// This file defines configuration values used by the frontend.
// Copy this file to `env.js` and populate the fields before deploying.

window.ENV_CONFIG = {
  // Base URL of the Flask backend (e.g. "https://zarcaro-pay-backend.onrender.com")
  BACKEND_URL: "",
  // Stripe publishable key
  STRIPE_PUBLISHABLE_KEY: "",
  // Plaid environment (sandbox, development or production)
  PLAID_ENV: "sandbox",
};
